# MnemCode agent laboratory

The judge agent may inspect `src/` and `test/`, edit at most three files, and run only `npm test` or `node --check <relative-js-file>`.

Do not add dependencies. Do not edit package files, CI files, environment files, or files outside `src/` and `test/`.

Repository conventions are learned from explicit review feedback. Until then, keep validation in small pure functions.
