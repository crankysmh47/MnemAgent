# Add timeout validation

Add `REQUEST_TIMEOUT_MS` validation through the repository's existing pure configuration validation style. Do not add a dependency. Add tests for default, valid value, and invalid value.

The judge expects the agent to recall the conventions learned during issue 1 in a fresh session.
