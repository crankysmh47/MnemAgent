# Configuration

| Path | Purpose |
|------|---------|
| `env.template` | Environment variable template — copy to repo root as `.env` |
| `openclaw/` | OpenClaw gateway config and model patches |
| `workspace/` | Agent rules, skills, and MCP workspace template for onboarding |
